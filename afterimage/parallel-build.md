# Afterimage substantial feature integration

Integration record for the installed September 25 creative workplace and Windows preview delivery. Source, build, package verification and Git closure are complete. Product authority: `AGENTS.md`, `module-manifest.md`, and `vault/projects/compositor/intent/shared-windows-art-workspace.md` in the shared workspace.

## Completed Windows preview delivery

The creative-workplace integration and native entry/identity packet are installed and pushed as `5c08175b5d`. The corresponding portable Windows preview is assembled and verified. An artist can unzip it and begin pixel art, a comic, a Temple composition, or AI-assisted artwork through the native welcome. Opening a workflow reaches native creation/editor controls without starting a paid provider request. Native document ownership and saved layouts remain authoritative.

| Owner | Writable boundary | Result |
| --- | --- | --- |
| Generation worker | `afterimage/scripts/package-windows.py`, `afterimage/PACKAGING.md`, packaging notices under `afterimage/` | Runnable portable package under ignored `.afterimage/releases/`, pinned runtime/license/source manifest, meaningful packaged-app startup/export evidence; no public upload |
| Artist worker | Native welcome widget and its owned UI/resources | Deliberate Afterimage first-use identity, working Pixel/Comics/Temple/AI entry actions, Open/recent files retained |
| Temple worker | `afterimage/branding/`, agreed icon/splash resource integration | Distinct Afterimage Windows/window icon and native startup splash, preserving Krita attribution and original resources |
| Root | Shared build/install, capture/runner interfaces, cross-plugin registration, integration review and Git | Review whole artist entry and portable workflow without physical desktop input |

Root retains the single native build lease. Coordinate changes to shared main-window, CMake or capture scripts before editing. The completed packet below records the contracts and evidence these delivery changes preserve.

The portable snapshot has passed real native comic startup and PNG export with loaded DLL paths restricted to its own package and Windows. Actual first-use review exposed malformed Artist dock metadata (nested JSON and the wrong plugin API version), so standalone Artist core workflows had passed while the human-facing docks were absent. Correct metadata and an explicit JSON-to-object build dependency now load the module. The final real sprite route passed: an editable transparent 64×64 document at 800%, 1-pixel brush, selected Pixel workplace retained across first-document layout restoration, and Comics hidden. Root viewed the 1440×900 native welcome and Pixel captures. Both exited 0 and isolated test configuration was restored byte-identically. The original Afterimage mark/splash and their source/license are installed while upstream artwork remains preserved.

Final artifact: `.afterimage/releases/Afterimage-0.1.0-preview+g5c08175b5d8a-windows-x64.zip`, 965,124,863 bytes, SHA-256 `9ff9438cad974f7c79f37b4c07cb685e2e435102b56e66719a4b8fd8529b154e`. All 4,781 payload-file hashes and archive CRC checks pass. The package's clean first-use Pixel capture (`final-package-pixel-first-use.png`) is pixel-identical to the accepted installed capture. Its module log (`final-package-first-use-modules.json`) records 271 package modules, including `kritaafterimageartist.dll`, and 111 Windows modules; none come from another installation or the development toolchain. An earlier package capture retained an old test layout; it is not first-use evidence. Test configuration was restored exactly after both runs.

This closes the development-preview delivery, not public release or human acceptance of the whole experience. Remaining public redistribution notice/source inventory belongs to `RELEASE-NOTICES.md`; specialist Temple behavior limits belong to its README. Frank's hands-on creative use is the next source of experience feedback. Local model inference remains an explicitly future provider. No public binary was uploaded and no visible app was opened on Frank's desktop for verification.

Frank wants a compelling public Windows art application, not prolonged work on tiny controls or migration from our disposable prototype. Build complete creative capabilities and verify integrated workflows. Do not take desktop focus, invoke Windows accessibility controls, open test windows, or restart the app while Frank works. Background/native tests may run without showing windows.

Krita owns document pixels, layers, selections, undo, color, saving and painting. Reuse its native tiled engine for large artwork. Providers produce retained image artifacts; authorized edits can apply directly and undoably, with comparison available when useful. There is no universal candidate-approval gate and no second live document engine. Original artwork stays preserved. The Glitch Temple source is read-only and must not be pushed or changed. No contact with Sazed is needed.

The agent controls the application through native commands and perceives artwork through document renders, independently of desktop visibility and physical input. Foreground, background and minimized operation must use the same path. Human typing, pointer input and window movement remain available. Changing the artist's selected document cannot retarget pending work. Binary images travel as typed image attachments or file references, never encoded text; document summaries remain compact with detail available on demand.

| Owner | Capability and writable boundary | Shared integration |
| --- | --- | --- |
| Temple agent | Complete Glitch Temple authoring, `plugins/dockers/afterimage_temple/` | Reuse the attributed original Studio in an explicitly opened wide native WebView2 surface. Preserve native document/render/apply ownership, normal window movement and minimization, and a compact quick-controls dock. Replace Tauri operations with working native bridges and app-owned storage. |
| Artist workplace agent | Multi-page comic creation, `plugins/dockers/afterimage_artist/` | Ordered local comic folder with editable KRA pages, bounded-memory multi-page PDF, selection-based wrapped lettering/balloons, shared native agent gateway. Preserve pixel and PDF fixes. |
| Generation agent | Explicit image providers, `plugins/dockers/afterimage/` including Dock/UI | Add distinct Google/OpenAI API routes and actual model selection, native credential handling, direct human generation plus shared native agent calls. Reuse edit capture, artifact retention and placement; preserve subscription/Sol defaults. Return verified provider contract before paid workflow calls. |
| Buddy root | Product architecture, cross-plugin registration, shared native code, build integration and combined review | Own parent CMake, shared application/layout files, module manifest and this map. Resolve cross-packet interfaces and review complete artist workflows. |

Each worker may revise local architecture. Coordinate with root before changing another boundary or a shared contract. Do not concurrently run configure/build/install against the shared build tree. Root assigns one build owner at a time. Root owns the shared native build lease for the active delivery packet. Use `afterimage/scripts/build-windows.py` for its isolated environment and canonical configure options. Temple may build its local web assets independently and must coordinate before native compilation. Independent offscreen runners may use completed executables. No bulk test snapshots or per-control testing expeditions. A completion report must distinguish implemented capability, actual evidence, and remaining gaps.

The current native code is available for reuse. It has a real app-server subscription connection, crop source/mask capture, retained alpha-bearing candidates, native masked placement and a full native save/reopen/export workflow check. File-based preview references replace base64 image text to avoid accidental huge model context. The recently added Compositor importer is no longer a product priority; spend no additional effort on migration.

Root will compile the combined native app, run substantive document/provider workflows in background, inspect integration and preserve a coherent commit. The integration scenario begins with a new chat and blank document, creates neutral original artwork, revises it, saves, exports and reopens. Political satire and prototype migration are not integration fixtures. Offscreen widget renders can support visual review without taking over the desktop. Frank clarified that he is working in other apps; the visible Afterimage process was Buddy's own development test instance. Root verified its exact executable and ignored trial-artwork argument, then closed it. A coherent development install may now replace that build. Do not activate desktop windows while Frank works.

## Earlier integration findings

The conversation packet is accepted after v5 offscreen visual review: distinct aligned messages, streaming follow/reading behavior, steering and explicit sticky artwork targets. The first real Sol creative run exposed blank vector previews despite accepted native edits. Its cause was native insertion order: shapes were inserted before their layer joined the image. The corrected path registers the layer first and uses Krita's shape creation command. The corrected Sol/Medium creative session completed native creation, actual image inspection, steering, targeted revision, KRA save/reopen and PNG export. Root viewed and accepted the artwork in `.afterimage/live-cat-sol-corrected/`. A single authorized subscription crop workflow now uses a copy, chooses advertised Luna/Medium if available, and records bounded model/context evidence.

The Temple persistent renderer now completes the originally failing full FORM composition. Processing's animation loop had stopped receiving later requests; a continuous animation-thread request loop fixes the actual cause. Native render/apply/undo/redo/KRA reopen and recipe recovery passed. Native authoring still has documented gaps; see the plugin README. The worker is connecting the service to chat and extending time/material authoring. No complete Temple parity or finished app claim follows from these results.

The initial packet is committed and pushed as `5e9a5d6059`, installed and verified through the actual executable. Explicit creation can open a new artwork while preserving the old one and rebind only the conversation; it clears the old crop capture. Image-generation provenance is captured per item when generation starts. Pixel/comic native workflows, row/strip exports, Temple's shared chat gateway, portable binary materials and real moving GIF/MP4 exports are complete at the scope described in their READMEs. The installed app's offscreen export preserves all vector lettering. No visible window or physical desktop control was used. Separate API image routes and remaining full Temple specialist authoring are the next product work; public release packaging remains distinct from the verified development install.

Root has now given Frank the installed executable link. No agent launched a new visible app. Any subsequently opened visible Afterimage instance may be Frank's; check before future installs and do not close it. The earlier closure authorization applied only to the exact old Buddy-owned trial process.

## Integration contracts and earlier review

The direct image page owns its provider, prompt and action; the conversation page owns the chat composer and model/reasoning controls. Provider setup is secondary to making artwork. New-image and edit jobs capture their destination when requested, and crop framing must survive provider aspect constraints without silent stretching. Gemini intermediate thought images are not final candidates. Provider requests remain cancellable without pretending that cancelling a local connection guarantees billing cancellation.

Comic saves update page ordering only after native background-save completion. Book export preserves individual page dimensions and DPI, renders nested vector layers, and loads one editable KRA page at a time without blocking the artist's input thread. Native document construction must obey Krita/Qt resource and thread ownership; moving the entire loader into a generic worker thread is not sufficient evidence that those contracts hold. A separate hidden export process is appropriate if native loading requires the application thread.

The full Temple Studio uses a normal top-level native window with minimize, maximize, move and close behavior. The original React controls remain the authoring surface; native Temple service operations continue independently of that window's visibility. The source UI's successful Vite build is not evidence that every original command has a working native replacement.

Native agent previews and edit capture accept explicit document-coordinate regions without changing the artist's live selection. Whole creative verification must use exposed agent operations; a test-only C++ selection cannot stand in for an agent capability.

Run native checks through `.afterimage/venv/Scripts/python.exe afterimage/scripts/run-native-check.py <build-executable-name> <arguments>` from the repository root. It supplies absolute build/install dependency paths, offscreen Qt, and process-local inherited Windows error handling so loader failures return to the log instead of opening desktop dialogs. Do not launch raw test executables or use hidden `Start-Process` as a substitute: hiding a console does not prevent DLL-error dialogs. A raw artwork-test diagnostic omitted its runtime PATH and produced `libpng16.dll` and Qt/KF5 missing-library dialogs on Frank's desktop. The dependencies were present; the corrected launcher completed its startup check successfully. Serialize linking a shared native DLL with checks that load it.

Use `run-native-check.py --installed afterimage.exe ...` to check the actual installed app with only its packaged DLLs and Windows system directories on PATH. For an actual Windows workspace render, `capture-workspace.py <KRA> <PNG> <resource-folder>` starts the whole process on a private desktop which is never switched into view. Both launcher and app verify its desktop before selecting the Windows Qt platform. The capture mode uses test configuration and a distinct instance key, renders the real application window after opening its KRA, then closes its native windows/documents normally. It cannot forward to or activate the artist's running app. Ordinary native checks retain the offscreen platform.

The combined packet passed the full native build and is installed. Its actual first-use workspace renders at 1440×900 with artwork, layers, brushes and the chat composer visible; the process exits successfully. Root reviewed that window, the real Gemini/OpenAI artwork and editable booklet, and the hosted Temple Studio. Native UI review exposed a null clipboard-data dereference, improper capture teardown, and a Temple preview/control minimum-size problem; those causes are corrected. Temple and Mutator now start hidden for new workspaces while saved layouts remain respected. Public release packaging and a deliberate Afterimage visual identity remain separate work.
