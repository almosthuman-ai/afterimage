# Afterimage module map

Status: Krita-based Windows development application installed, with a curated portable preview verified outside the development prefix. This is not a public binary release. Original Compositor is preserved. Origin: https://github.com/almosthuman-ai/afterimage. Upstream: https://invent.kde.org/graphics/krita.git. Starting source: 188d77827a (2026-09-24).

## Goal

A beautiful, responsive Windows creative workplace where pixel artists, comic creators, glitch artists and people making everyday graphics can finish work they are proud of. Human tools and the AI collaborator share the actual artwork. Large documents, sustained experimentation, editable text, original sources and reliable recovery are first-class design conditions.

## Owners

| Work | Source | Boundary |
| --- | --- | --- |
| Native application and identity | `krita/`, `libs/ui/` | Existing Krita application/document ownership; separate Afterimage identity and per-user state |
| Painting, images, history, scheduling | `libs/image/`, `libs/pigment/` | Preserve the native tiled engine and artistic semantics |
| Native collaboration | `plugins/dockers/afterimage/` | Conversation and steering, app-server subscription, explicit models/reasoning, separate API image providers, native commands/renders, immutable selection or region capture, retained generation and native placement |
| Glitch creative integration | `plugins/dockers/afterimage_temple/` | Original Studio in a native WebView2 window, shared document-bound agent gateway, preserved renderer, undoable layers, portable materials, artist-owned symbol libraries and loops |
| Pixel/comic workplaces | `plugins/dockers/afterimage_artist/` | Transparent sprites, palette layers, editable panels and fitted lettering, ordered KRA books, streaming PNG and isolated background PDF export; shared native agent gateway |
| Build and packaging | `afterimage/scripts/build-windows.py`, `package-windows.py`, upstream CMake | Isolated Windows toolchain/dependencies, curated portable runtime and hashed manifest under `.afterimage/`; no public upload |

## Integration sources

Glitch Temple remote `https://github.com/taimei886/glitch-temple.git`, source revision `f4cde5d`; local reconciliation `9a1a7de` retains the earlier Tai Mei MIT attribution commit. The pull included 56 upstream commits and current declared version 0.47.3. Source has materially richer FORM, palette, ASCII/Language Body, audio-bending, targeting and time/material workflows than the old Compositor copy. Coverage must be grounded in that revision, not the old copied effect catalog.

Existing Compositor owns historical `.compwin` and `.compbook` files, provider artifacts and chat state. Those originals remain preserved. Migration from this disposable prototype is not a product priority.

## Current packet

Windows LLVM-MinGW 20251118 (Clang 21.1.6), Python 3.13 and CMake 3.31.10 are installed under `.afterimage/`; upstream Qt5 dependencies are downloaded. The full native build, including Python bindings, has passed and installed into `.afterimage/_install`. Preserve `PROCESSOR_ARCHITECTURE` in the isolated build environment and select `XSIMD_ARCH=x86-64`; omitting processor information caused missing optimized drawing implementations at link time. SIP 6.10 is installed in the local venv because prebuilt SIP launchers are not relocatable. PyQt 5.15.11 comes from upstream dependencies.

The collaborator dock implements New Chat, paginated history, sign-in, streaming, inline questions/approvals, document inspection and generation candidate comparison and placement through native layer/selection commands. Model and reasoning controls are always explicit: factory defaults are GPT-6-Sol and Medium, and Make default stores a user choice for new chats. The signed-in catalog supplies the available models and supported reasoning levels; Astra is never an implicit fallback. `AfterimageSession` owns JSON-RPC and app-owned authentication. The bundled official Codex 0.156.0 runtime preserves its complete vendor layout; sign-in does not borrow another application's account files. Tests inject an isolated runtime and state directory directly: changing QSettings' default format does not isolate the explicit organization/application constructor on Windows.

`AfterimageDocumentBridge` captures shared native tiles under the image scheduler, then renders bounded canvas/selection previews on a worker. Its layer rename and opacity operations use native undo commands and report completion after the scheduler finishes. Edit-area capture retains a bounded source PNG, exact document rectangle and compressed native selection tiles, including mask origin and default pixel. Generated pixels are converted and resized through the native tiled engine off the UI thread. Placement attaches the paint layer and editable transparency mask as one subtree and one undo operation, preserving the captured selection even after the artist changes the live selection. A turn keeps its original document even if the artist changes tabs. `AfterimageImageStore` retains provider originals, alpha-bearing PNGs, thumbnails and source previews with provenance on worker threads. Encoded cloud results and local file artifacts use the same store; local model inference is not implemented.

Earlier standalone native checks covered chat lifecycle, stopping, reopening, reconnection, history order, cross-thread notification filtering, alpha/provenance preservation, source-preview retention and settings isolation. The native placement fixture covered a feathered selection with a hole, immutable source/mask retention, placement after selection changes, unchanged pixels outside the crop, generated alpha, undo/redo, KRA save/reopen and PNG export. A historical live political-art crop attempt encountered provider output moderation and is not evidence of a successful editing workflow. Current creative evidence uses newly created neutral artwork. Separate API routes and the hosted full Temple Studio have real workflow evidence below.

The current integration replaces the transcript-like chat with distinct human/assistant messages and usable streaming/steering. It also expands the native document command surface so an agent can create and revise artwork without desktop automation. Agent commands bind to a document rather than the artist's current tab, and image perception comes from document renders. Preview files and typed image inputs replace encoded image text; layer inspection is paginated. See `afterimage/parallel-build.md` for the active worker boundaries. Background builds and document/widget renders must not seize Frank's desktop. The old visible development instance belonged to Buddy, not Frank; it has been closed so the coherent build can be installed.

The corrected real GPT-6-Sol/Medium session completed a neutral three-panel comic from a blank document: native editable vector layers, rendered image inspection, accepted steering, targeted revision, KRA save/reopen and PNG export. Root reviewed the final artwork. Artifacts and bounded semantic evidence are under ignored `.afterimage/live-cat-sol-corrected/`. This exposed and corrected a real shape-registration bug that metadata-only checks had missed. That vector session did not by itself establish provider image editing.

The subsequent real GPT-6-Luna/Medium subscription workflow completed image generation, exact asynchronous candidate retention, placement through the captured native selection, KRA save/reopen and PNG export on a copy of that comic. Evidence lives in `.afterimage/live-cat-subscription-edit/`. Native tool text was at most 922 bytes; per-step context was 17,736–19,920 tokens. The 172,289-token cumulative report includes 148,224 cached tokens across repeated model steps, not a single giant input. Root reviewed a real quality failure: a requested additive sparkle inherited the instruction to regenerate an opaque crop, leaving a faint background patch. The instruction now distinguishes transparent foreground additions from crop replacements; that correction has not had another paid generation trial, and the retained sample is not claimed to have alpha.

Artist workflows now create a real colored transparent sprite, palette-reduce it, export original/nearest-scaled indexed PNG, save/reopen native KRA, and create/export/reopen editable comic panels and lettering plus PDF. PNG writes bounded rows; PDF uses bounded strips with explicit zero page margins. Root reviewed actual PNGs, a raster of the exported PDF, and the 360px dock renders. Temple's native workflow covers render/apply/undo/redo/reopen, binary materials restored from KRA after the local source disappears, and real GIF/MP4 output with differing decoded frames. Both workplaces are available through Tools → Creative Workplaces and callable from chat against its bound document. Their READMEs own the remaining capability gaps.

The full build and development install pass. The installed executable itself exported the neutral comic offscreen with all vector lettering intact; the result matches the authored PNG apart from negligible raster rounding. That deployment check exposed two upstream display-free failures now corrected: a null Windows OpenGL probe dereference, and batch export snapshotting before vector paint caches initialized. Processing 4.5.6, FFmpeg 8.1.2 and the Codex 0.156.0 vendor tree are installed with runtime notices; see `afterimage/THIRD-PARTY-dev-runtime.md`. No visible app was launched for these checks. Native/session/Artist/Temple workflow suites pass; these establish functioning native capabilities, not full Temple parity or a finished public release.

The installed integration extends baseline commit `5e9a5d6059` with `AfterimageApiImages`, direct Google/OpenAI image controls and Windows Credential Manager keys, explicit document-coordinate crop regions that leave the artist's selection untouched, and cancellable generation with bounded artifact references. A real neutral comic workflow used `gemini-3-pro-image` for the original artwork and `gpt-image-2.5-sunburst` for the middle-panel region edit, then saved and reopened editable KRA and PNG. Root viewed the result in `.afterimage/live-api-rooftop/`; both provider results were intentionally opaque replacements. Provider details and exact evidence belong to `plugins/dockers/afterimage/README.md`. The combined provider, comic and full Studio packet has passed the full native build and is installed with the comic export helper, Studio assets, WebView2 loader and upstream notices.

`ComicProject` owns only ordered KRA filenames in a readable folder manifest. Native save completion precedes manifest updates. `afterimage_comic_pdf_worker` loads one page at a time in its own offscreen process and Qt main thread, preserving native resource ownership, nested vectors and each page's DPI while the editor remains usable. Progress and Stop stay in the dock; a completed PDF replaces the destination atomically. The mixed-DPI fixture and the real provider-artwork booklet have both been visually reviewed. `.afterimage/live-api-rooftop/booklet/` contains three editable KRA pages and the ordered 256×432 pt PDF; each retains artwork and separate vector captions, and the middle page retains the provider edit as a separate native layer. The corrected strip copy has clean caption placement; its original remains untouched.

The original Glitch Temple React Studio is preserved with attribution under `studio_web/`. `TempleStudioHost` is an explicitly opened normal native window; `TempleStudioBridge` maps its commands onto native documents and app-owned storage. The native bridge workflow passed imported material, FORM authorship, explicit export dimensions, document-size layer placement, KRA save/reopen and portable recipe checks. The actual hosted React FORM workflow also passed: internal UI action, WebView2 preview, native Apply, recipe annotation and KRA save. Root viewed the retained Studio capture in `.afterimage/temple-probe/studio-host/`. The proof launches its whole process tree on a never-switched private desktop, because moving only the Qt thread leaves WebView2 children on the wrong desktop. Artist-owned image-folder import populates the original searchable symbol browser in a worker thread; deduplication, listing and selection passed. The personal upstream corpus is not bundled. Source command routes are implemented; every specialist branch has not been individually exercised, and Canvas2D previews can differ from Processing exports.

The installed application now has a real workspace-capture path through normal `KisApplication` startup on the Windows Qt platform. `afterimage/scripts/capture-workspace.py` owns a private process desktop, invokes the installed native-check runner, and retains only the window's own rendered pixels. It never switches desktops or takes physical input. Capture uses separate test configuration, opens the supplied KRA, and closes documents while Qt is still alive. Debugger evidence exposed an upstream null clipboard-data dereference during window creation; `KisClipboard` now treats absent data as an empty clipboard. The compact Temple dock scrolls its specialist controls and fits its preview without forcing the main window to image-sized minimum dimensions. Temple and the optional Mutator start hidden in a new workspace; saved artist layouts still restore. Root reviewed the actual installed first-use window at exactly 1440×900 in `.afterimage/workspace-proof/afterimage-native-first-use-final.png`, with artwork, native layers/brushes and the conversation composer visible. The process exited successfully. Only the generated Qt test configuration was temporarily backed up for this proof and restored byte-identically; the artist's real configuration was untouched.

All new native checks run through `afterimage/scripts/run-native-check.py`. It supplies absolute dependency paths and offscreen Qt, and inherits process-local Windows error handling so startup failures reach the log rather than interrupting the desktop. An earlier raw diagnostic omitted the runtime PATH and caused missing-DLL dialogs although the libraries existed. The corrected launcher passed startup and subsequent provider/Temple checks. Do not use raw executable or hidden-console launches for background checks; serialize DLL linking with checks that load those libraries.

The Windows delivery packet adds the native Afterimage welcome, original application icon/splash, and portable packager. Actual welcome-to-sprite use exposed malformed Artist plugin metadata: the underlying tools passed their workflows while the human-facing dock module was skipped. The metadata now matches Krita's dock contract and plugin API version 28; an explicit JSON build dependency ensures it reaches the DLL. Optional Pixel/Comics docks start hidden. An explicitly chosen welcome workplace survives first-document layout restoration, while passive agent presentation retains its existing focus behavior. Creating a sprite selects the existing 1-pixel brush and a fitting integer zoom. Root reviewed the actual 1440×900 window in `.afterimage/workspace-proof/afterimage-pixel-entry-final.png`: editable transparent 64×64 Pixels layer, 1 px brush, 800% zoom, selected Pixel workplace visible, Comics hidden. The final welcome capture also passed; both processes exited 0 and the isolated test configuration was restored byte-identically.

`package-windows.py` stages runtime DLLs, native plugins/resources, Python, complete Codex vendor files, Processing/JDK, FFmpeg and Temple Studio with a per-file hash manifest. It omits compiler tools, headers, import libraries, debug symbols and personal account/artwork state. The curated preliminary snapshot is 1.92 GiB. Its real native comic startup and 1536×864 PNG export passed; root reviewed the artwork/lettering. Module logs under `.afterimage/releases/` contain only package and Windows paths, with no development-prefix dependencies. Final packages live under the same ignored releases folder. The packager's manifest identifies the checkout and exact binary hashes without claiming a reproducible build attestation. `RELEASE-NOTICES.md` records the remaining component notice/source work for public binary redistribution. The external Microsoft WebView2 Runtime is required for full Temple Studio.

AfterimageCompositorImport imports version-1 Compositor documents into native raster, text/vector and group layers with transparency masks, visibility, opacity and locks. Original metadata and provider provenance remain in a KRA annotation. Unsupported transforms, blending, clipping, effects and layer kinds produce an explicit error rather than flattening. Text stays editable, but its spacing must be reviewed across the different text renderers. The actual Nothing to Recognize Here document migrated, saved and reopened with all 17 layers, 13 editable text layers, 2 masks and its hidden OpenAI alternative; original Compositor files remain untouched.

Renaming the application requires the `afterimagerc` embedded resource alias in `krita/krita.qrc`; otherwise KConfig cannot find the shipped default layout and starts with hidden toolbars and docks. The first Afterimage workspace adds the collaborator beside the native painting layout; later launches retain the artist's arrangement. The native welcome now identifies Afterimage and opens Pixel art, Comics, Glitch Temple, and AI creation workflows while keeping Open and recent artwork. It uses the existing native creation forms and never sends a provider request itself. The Windows executable/window icon and native startup splash now use the original Afterimage image-plane mark in `afterimage/branding/`. Editable SVGs and CC-BY-SA-4.0 attribution/license install with the app; Krita's original assets and credits remain preserved.

Commands, from this repository: `.afterimage/venv/Scripts/python.exe afterimage/scripts/build-windows.py configure`, `build`, `install`, or `test-session`. `prepare-dev-runtime.py --codex-package <official-package-root>` installs the Codex vendor tree and C++ runtime DLLs. `launch-dev.py [artwork-path]` opens the native app with only installed runtime dependencies on PATH. `check-app-server.py --runtime <codex.exe>` checks the real protocol without authentication or model calls. Build logs and generated state stay under `.afterimage/`. Afterimage has its own application name, executable, settings and diagnostic log names; resource compatibility retains upstream `share/krita` paths and attribution. The portable preview packager and notices are described in `afterimage/PACKAGING.md` and `afterimage/RELEASE-NOTICES.md`; the latter owns remaining public redistribution notice work.

The unused historical `../compositor-krita` clone is excluded from workspace VS Code repository discovery, watching and search. Both clones ignore generated toolchains/dependencies. The active source repository is `afterimage`.
