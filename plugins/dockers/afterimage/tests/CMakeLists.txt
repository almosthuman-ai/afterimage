cmake_minimum_required(VERSION 3.16)
project(AfterimageSessionTests LANGUAGES CXX)
set(CMAKE_AUTOMOC ON)
set(CMAKE_CXX_STANDARD 17)
find_package(Qt5 REQUIRED COMPONENTS Core Gui Test Concurrent)
add_executable(afterimage_session_tests SessionTest.cpp ../AfterimageSession.cpp ../AfterimageImageStore.cpp)
target_link_libraries(afterimage_session_tests Qt5::Core Qt5::Gui Qt5::Test Qt5::Concurrent)
target_compile_definitions(afterimage_session_tests PRIVATE QT_USE_QSTRINGBUILDER QT_NO_SIGNALS_SLOTS_KEYWORDS)
enable_testing()
add_test(NAME afterimage_session COMMAND afterimage_session_tests)
